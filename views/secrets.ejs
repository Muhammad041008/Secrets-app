<!DOCTYPE html>
<html lang="en">
<head>
    <link rel="stylesheet" href="/style.css">
    <title>Secrets</title>
</head>
<body>
    <h1>Your Secrets</h1>

    <!-- Display the saved message -->
    <% if (secretMessage) { %>
        <h3>Your Saved Message:</h3>
        <p><%= secretMessage %></p>
    <% } else { %>
        <p>You haven't saved any message yet.</p>
    <% } %>

    <!-- Form to save a new message -->
    <form action="/secrets" method="POST">
        <label>Write Your Reason:</label><br>
        <textarea name="message" rows="5" cols="30" placeholder="Write your secret message here..." required></textarea><br>
        <button type="submit">Save</button>
    </form>

    <a href="/logout">Logout</a>
</body>
</html>
